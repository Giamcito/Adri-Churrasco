# Example environment variables (no real secrets)
# Copy to .env locally and fill with real, private values (never commit .env)

# Backend only: Stripe secret key (DO NOT USE IN FRONTEND)
# STRIPE_SECRET_KEY=sk_test_yourKeyHere
